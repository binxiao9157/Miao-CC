===================================================================
Batch 3: Performance Optimizations (3 files)
===================================================================

--- a/src/pages/Home.tsx
+++ b/src/pages/Home.tsx
@@ imports
-import { useState, useEffect, useRef, TouchEvent, RefObject } from "react";
+import { useState, useEffect, useRef, useMemo, TouchEvent, RefObject } from "react";

@@ before return (line ~433) - Added useMemo for video elements
+  const actionVideoEntries = useMemo(() => {
+    if (!cat?.videoPaths) return null;
+    return Object.entries(cat.videoPaths).map(([key, url]) => {
+      if (!actionRefs[key]) return null;
+      return (
+        <video key={key} ref={actionRefs[key]} src={url} muted playsInline
+          preload="auto" onEnded={() => setActiveAction(null)}
+          className={`... ${activeAction === key ? 'opacity-100' : 'opacity-0'} ...`}
+        />
+      );
+    });
+  }, [cat?.videoPaths, activeAction]);

@@ render (line ~474) - Replaced inline map with memoized variable
-        {canLoadActions && cat?.videoPaths && Object.entries(cat.videoPaths).map(...)}
+        {canLoadActions && actionVideoEntries}

--- a/src/context/AuthContext.tsx
+++ b/src/context/AuthContext.tsx
@@ imports
+import { useMemo, useCallback } from 'react';

@@ state - hasCat changed from independent state to derived
-  const [hasCat, setHasCat] = useState(false);
-  const refreshCatStatus = () => { setHasCat(storage.getCatList().length > 0); };
+  const [catCount, setCatCount] = useState(0);
+  const hasCat = useMemo(() => catCount > 0, [catCount]);
+  const refreshCatStatus = useCallback(() => { setCatCount(storage.getCatList().length); }, []);

@@ logout
-    setHasCat(false);
+    setCatCount(0);

--- a/src/services/storage.ts
+++ b/src/services/storage.ts
@@ getUserKey cache - Replaced per-call localStorage reads with cached prefix
-let cachedCurrentUser: UserInfo | null = null;
-let cachedCurrentUserRaw: string | null = null;
-const getUserKey = (key: string) => {
-  const currentUserRaw = localStorage.getItem(STORAGE_KEYS.CURRENT_USER);
-  ... (reads localStorage every call)
-};
+let cachedUserPrefix: string = 'guest';
+let cachedCurrentUserRaw: string | null = null;
+const refreshUserPrefix = () => { ... updates cachedUserPrefix from localStorage ... };
+const getUserKey = (key: string) => {
+  if (cachedCurrentUserRaw === null) refreshUserPrefix();
+  return `${cachedUserPrefix}_${key}`;
+};

@@ saveUserInfo - Added refreshUserPrefix() call
@@ clearCurrentUser - Added refreshUserPrefix() call
