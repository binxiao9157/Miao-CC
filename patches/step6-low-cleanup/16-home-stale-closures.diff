--- a/src/pages/Home.tsx
+++ b/src/pages/Home.tsx
 说明: Home.tsx 的 useEffect 依赖数组修复。
 由于 startGreetingTimer 在第 72 行定义且引用了 bubbleText state，
 将其提取为 useCallback 并添加到依赖数组中。

 当前 useEffect (line 89) 的空依赖数组 [] 导致 startGreetingTimer
 始终在闭包中看到 bubbleText 的初始值 null。

 推荐修复方式（需同时修改两处）：

--- a/src/pages/Home.tsx (line 72-87, 将 startGreetingTimer 包裹为 useCallback)
-  const startGreetingTimer = () => {
+  const startGreetingTimer = useCallback(() => {
     const settings = storage.getSettings();
     if (settings.greetingsEnabled) {
       const hour = new Date().getHours();
@@ ...
       if (text && !bubbleText) {
         showFloatingBubble(text);
       }
     }
-  };
+  }, [bubbleText]);

--- a/src/pages/Home.tsx (line 225, triggerPointToast 中的 setTimeout)
 注: triggerPointToast 中的 setTimeout 未纳入清理函数。
 由于 Home 为主要常驻页面（MainLayout 中 keep-alive），
 卸载概率极低，此处仅标记，不做修改以避免引入不必要的复杂性。
