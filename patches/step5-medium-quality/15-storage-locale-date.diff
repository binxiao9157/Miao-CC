--- a/src/services/storage.ts
+++ b/src/services/storage.ts
 说明: 将 storage.ts 中用于日期比较的 toLocaleDateString() 替换为
 toISOString().slice(0, 10)，确保跨 locale 一致性。

 同样需要在 Home.tsx 和 Points.tsx 中保持一致。

--- a/src/services/storage.ts (line 446)
-    const today = new Date().toLocaleDateString();
+    const today = new Date().toISOString().slice(0, 10);

--- a/src/pages/Home.tsx (line 101)
-    const today = new Date().toLocaleDateString();
+    const today = new Date().toISOString().slice(0, 10);

--- a/src/pages/Home.tsx (line 232)
-    const today = new Date().toLocaleDateString();
+    const today = new Date().toISOString().slice(0, 10);

--- a/src/pages/Points.tsx (line 38)
-  const today = new Date().toLocaleDateString();
+  const today = new Date().toISOString().slice(0, 10);

注意: 首次应用此补丁后，已记录的 lastLoginDate/lastInteractionDate
会因为格式变化而被视为"不同日期"，触发一次额外的每日登录奖励。
这是一次性的自愈行为，不影响后续使用。
